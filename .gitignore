# Security scan reports
*-report.json
*-stderr.txt
precommit-scan-*.md
security-report-*.html
security-report-*.md

# Claude Code local state (gitignored — reproduced by bootstrap.sh)
.claude/settings.json
.claude/settings.local.json
.claude/git-workflow.local.yml
.claude/git-report.html
.remember/

# git-orchestrator personal identity (never commit — use .local.yml.example as template)
# (entry above in Claude Code local state section)

# BMAD local config (not pushed)
_bmad/

# Serena MCP local state
.serena/

# OS
.DS_Store
